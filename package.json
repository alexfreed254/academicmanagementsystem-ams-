{
  "name": "ttti-ams-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "build:frontend": "npm --prefix frontend ci && npm --prefix frontend run build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "containers:list": "wrangler containers list",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.30"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250712.0",
    "typescript": "~5.7.2",
    "wrangler": "^4.28.0"
  }
}
