{
  "name": "ttti-ams-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo Use npm run dev from the repo root && exit 1",
    "deploy": "echo Do not deploy workers/wrangler.toml. Use npm run deploy from the repo root. && exit 1",
    "check": "tsc --noEmit",
    "tail": "wrangler tail --config ../wrangler.jsonc"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "hono": "^4.6.0",
    "scrypt-js": "^3.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "~5.7.2",
    "wrangler": "^3.90.0"
  }
}
