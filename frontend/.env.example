# Workers API base URL.
# Leave EMPTY for Cloudflare Worker Assets + Flask Container (same origin).
# Example only if API is split: https://api.yourdomain.ac.ke
VITE_API_BASE_URL=

# Optional override when Flask is on another host (rare with Containers setup)
VITE_LEGACY_ORIGIN=

VITE_SOCKET_URL=
